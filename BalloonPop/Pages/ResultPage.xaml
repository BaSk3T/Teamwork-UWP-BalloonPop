<Page
    x:Class="BalloonPop.Pages.ResultPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BalloonPop.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="ScoreTemplate">
            <TextBlock Text="{Binding ScoreAsText}" FontSize="16" Foreground="Gold"/>
        </DataTemplate>
    </Page.Resources>

    <StackPanel Background="Black">
        <TextBlock Text="Results" VerticalAlignment="Top" HorizontalAlignment="Center" FontSize="40" Foreground="Purple"/>
        <ItemsControl ItemsSource="{Binding Scores}"
                      ItemTemplate="{StaticResource ScoreTemplate}"
                      VerticalAlignment="Center"
                      HorizontalAlignment="Center"/>

        <Button Background="Purple" Foreground="Yellow" Name="GoToMenu" Click="SwitchToMenu" Content="Menu" Width="150" HorizontalAlignment="Center"/>
    </StackPanel>
</Page>
